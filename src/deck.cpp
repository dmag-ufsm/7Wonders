#include <list>
#include "deck.h"

namespace DMAG {

    Deck::Deck() = default;

    Deck::Deck(unsigned char age, unsigned char number_of_players){
//        this->push_front(Card(CARD_ID::MATERIALS::lumber_yard,"Lumber Yard",));
    }
}